> Part of the 2026-07-16 north-star unblock swing. Exploration tier; the attached referee report GOVERNS (grades + corrections + firewall verdict). Firewalled lanes (B, C) were audited INDEPENDENT.

All computations verified. Deliverable follows.

---

# DELIVERABLE 1 — FRAME R: The Access-Constitutive Normalization Frame, v0.1

**Author lane:** independent rival author (firewalled; no receiver diagnostics read)
**Tier:** exploration. **Grade of every claim in this document:** philosophical-argument grade unless marked otherwise; nothing here is a theorem, and nothing here moves any source-repository claim.
**Proposed path (DO NOT auto-place; receiver decides final location):** `possibility-to-capability/packets/rival-frames/FRAME-R-access-constitutive-v0.1.md`

## R.0 Role statement

Frame R is the strongest construction of operational-context relativism, authored by its own advocate, to serve as the "context-constitutive alternative" required by the Rank-2 paired-intervention test's prerequisites ("at least two independently defended normalization frames, including the strongest access-constitutive construction"). R is offered frozen and in advance of any outcome classification. R roots for itself, not for the receiver's hierarchy, and states its own concession and falsification conditions below.

## R.1 Definition of capability (access-constitutive)

**Definition (R-capability).** Let S be a system and let I be an *interface*: a declared, jointly specified triple of (a) observation channels (what about S can be registered), (b) control channels (what can be done to S), and (c) a verification procedure (how task success is certified). Then

> Cap_R(S | I) = the set of task-instances (t, v) such that t can be *posed* through I's control channels and success *certified* through I's verification procedure v, and S succeeds.

There is no Cap_R(S). The unrelativized "task set the system can perform" is not an idealization of Cap_R(S | I); it is a different kind of object — a God's-eye task ontology — and R holds that no operational content attaches to it. The point is not epistemic ("we can only *know* capability through access") but constitutive: a task is *individuated* by its verification conditions, verification is access, therefore the task ontology inherits access-relativity before any question of knowledge arises. "Capability minus access" is not capability purified; it is a change of subject.

Three consequences fix R's shape:

1. **No canonical completion.** Any "normalized" capability requires a canonical or maximal interface I* against which access differences are quotiented out. Either I* is a specific interface (then normalization is relative to a choice, and the choice is access), or I* is a limit over all interfaces (then its existence and uniqueness are exactly what must be proven, by universal property, not assumed).
2. **Coupled-system attribution.** Capability is a property of the pair (S, I), the way an affordance is a property of an animal-environment pair, not of the cliff or the animal alone.
3. **Facts, not just reports, are interface-relative** in R's strong reading (see R.3): what a system "did" is itself only defined relative to an interaction context.

## R.2 Classification rule for any paired intervention

Given a paired intervention (before-state B, after-state A) over a declared shared task vocabulary V, with declared interfaces I_B, I_A:

**Rule R1 (primary).** If the *verified success profile* differs — i.e., Cap_R(S|I_A)∩V ≠ Cap_R(S|I_B)∩V — then the intervention is a **CAPABILITY_CHANGE**. This includes every intervention the rival frame would call "access-only": enlarging observation or control channels enlarges what can be posed and certified, hence enlarges capability. R does not possess ACCESS_CHANGE as a contrast class at the same level. This is not evasion; it is R's substantive claim.

**Rule R2 (R's own internal distinction — the honest replacement).** R does draw a distinction, but a different one, drawn entirely in interface-relative terms:

- An intervention is a **CONSERVATIVE INTERFACE EXTENSION** iff I_B ≤ I_A (channel-wise inclusion) and the after-profile *restricted to the before-interface* equals the before-profile: Cap_R(S|I_A)|_{I_B} = Cap_R(S|I_B). New things are doable/certifiable; nothing formerly doable changed.
- An intervention is a **NON-CONSERVATIVE CHANGE** iff the restriction fails, or iff the profiles differ at a fixed interface (I_B = I_A but profiles differ).

R predicts the receiver's ACCESS_CHANGE/CAPABILITY_CHANGE boundary, wherever it is stable, will turn out to be a *relabeling* of conservative-extension/non-conservative-change — a distinction R already owns without any God's-eye ontology — and that where the two boundaries diverge, the receiver's labels will be completion-dependent (see R.4).

**Rule R3 (ill-formedness).** R declares the question "same capability, different access?" **not well-formed** when no common interface exists through which V can be both posed and certified in B and in A — i.e., when the comparison requires evaluating S's success on tasks that cannot be verified in one of the two conditions. In that case there is no shared measurement context, and the classification question has no truth-value rather than an unknown one. (This is R's analogue of the Rank-2 outcome "no shared task comparison is meaningful.") Note carefully what R does *not* say: R does not declare ill-formedness merely because access differs — that would trivialize the test. Ill-formedness requires failure of common posability/certifiability of V itself.

**Rule R4 (out-of-model access audit).** When the paired cases are *formal models* (as in the TI case below), R requires the classifier to declare which facts are certified only by God's-eye access to the model definition (e.g., "the issuance structure was held fixed") versus facts certified through an in-model interface. R predicts the "held fixed" leg of every paired intervention is certifiable only from outside every in-model interface — which is R's point made flesh.

## R.3 Strongest philosophical grounding

R's advocate has four candidate foundations and ranks them; the strongest is a three-layer argument with **Bridgmanian operationalism as the semantic core, relational quantum mechanics as the physical precedent, and a contextuality (Kochen–Specker-style) obstruction as the formal weapon.** Enactivism and Gibsonian affordances are supporting precedents, not the load-bearing layer, because both concern organism-scale cognition and invite the (unfair but effective) charge of biologism; R declines to rest on them.

**Layer 1 — semantic (Bridgman, properly reconstructed).** Bridgman's claim, in its defensible modern form, is not the crude verificationism that died with logical positivism; it is that a *concept applied beyond the operations that define it is a new concept requiring new warrant*. "Length" defined by rods and "length" defined by light-signals are distinct concepts that happen to agree in an overlap regime; extrapolating either past its operational domain is a substantive posit, not an analytic continuation. Apply this to "capability": the concept is anchored in test procedures — pose a task, grant resources, certify success. A "capability the system has, independent of any procedure by which the task could be posed or certified" extends the concept past every operation that gives it content. The rival normalization frame does exactly this extension when it "subtracts access": it assumes the concept survives the subtraction with its identity intact. Bridgman's razor says: show the new operations that define the subtracted concept, or admit you have changed the subject. The analytic-metaphysics literature on dispositions independently corroborates this: finks and masks show that even "intrinsic disposition" resists context-free analysis — the simple conditional analysis fails precisely because test-context contributions cannot be cleanly subtracted.

**Layer 2 — physical precedent (relational QM).** RQM (Rovelli) holds that the values of physical variables are relative to interactions: there is no interaction-free fact of the matter about a variable's value, and this is a statement about *facts*, not about knowledge. This matters to R because the receiver's program is physics-adjacent (frozen packets about C-operators, spectral sections, issuance). If the best available reading of our most tested physical theory already relativizes *value-facts* to interaction contexts, then a normalization frame that posits interaction-free *capability-facts* is asserting something the physics does not supply and arguably resists. R does not claim RQM is proven; R claims the burden of proof is inverted — the separation frame, not R, is the one importing a metaphysical extra.

**Layer 3 — formal weapon (contextuality obstruction).** The Kochen–Specker theorem: for Hilbert dimension ≥ 3, there is no noncontextual assignment of definite values to all observables consistent with the functional relations QM imposes. Structural analogy, stated as a precise prediction rather than a metaphor: an access-subtracted capability profile is a *global assignment* of doable/not-doable to all tasks in V, required to be independent of the measurement (interface) context in which each task is embedded, and required to restrict correctly to every actually-available interface. KS shows that in at least one physically real regime, globally consistent noncontextual assignments of this logical shape *do not exist*, even though every single context has perfectly definite local facts. R therefore predicts (R.4, P3) that sufficiently rich paired-case families will exhibit contextuality-like obstructions: locally classifiable everywhere, globally unclassifiable — no single access-independent capability assignment consistent with all interface restrictions. If the receiver's cases are always globally patchable, that is genuine evidence against R (R.6c), because it means the case domain is relevantly *unlike* the one regime where we know noncontextual global assignment fails.

Grade of R.3: philosophical-argument grade; the KS analogy is a structural claim whose bite in any given case family is an empirical/formal question, and R stakes falsifiable content on it rather than treating it as decoration.

## R.4 Predictions

For a **generic access-enlargement** (a paired case whose ALPHA leg widens observation/control with the underlying mechanism nominally fixed), R predicts the receiver will find:

- **P1 (free parameter).** Every operationalization of "normalized task capability held equal" contains an undischarged canonical-interface assumption, visible as a free parameter: a completion class, a maximal-access stipulation, or a privileged decomposition of the system into "mechanism" and "channel." The receiver will be able to state the assumption but not derive it.
- **P2 (label flipping under admissible completions).** For at least some paired cases, two admissible completions of the interface (both consistent with the frozen evidence) will assign *different* labels — ACCESS_CHANGE under one, CAPABILITY_CHANGE under the other — i.e., no invariant quotient. This maps to the Rank-2 outcome "equally admissible frames disagree with no invariant quotient."
- **P3 (contextuality signature).** In case families rich enough to embed incompatible interface contexts, local classification will succeed in every context while global access-subtracted assignment fails (R.3, Layer 3).
- **P4 (out-of-model certification).** In formal paired cases specifically, the "structure held fixed" certificate for the ALPHA leg will be checkable only by God's-eye access to the model text, never through any in-model observer interface; and the BETA leg will admit parameter regimes in which a genuine structure change is *invisible* through every declared in-model interface (the fixed projection cannot register it). R flags in advance: the TI computation below exhibits exactly this (BETA leaves the L=2 projected record and the observer's entire decidable set bit-for-bit unchanged while the issuance set grows by four elements). R's reading: the capability/access separation in such cases is being adjudicated entirely at a level *no access in the model reaches* — which is R's thesis, exhibited rather than refuted.
- **P5 (conservative-extension coincidence).** Where the receiver's labels are stable, they will coincide extensionally with R's conservative-extension/non-conservative distinction (R.2, Rule R2), showing the stable content never required the God's-eye ontology.

**What evidence would make R concede that a completion-stable capability/access separation exists (concession conditions, stated by R's advocate in advance):**

R concedes — for the tested class, which is the only scope the claim can have — if the receiver exhibits all of:

- **C1.** An explicitly characterized class **𝒞** of admissible interface completions (not one preferred completion), with a stated closure/exhaustion rationale, such that the ACCESS/CAPABILITY classification of every preregistered paired case is **invariant under all of 𝒞** and under admissible representation changes.
- **C2.** At least one nontrivial case landing stably in *each* class (a stable ACCESS_CHANGE and a stable CAPABILITY_CHANGE), with genuinely distinct preregistered predictions that came out as predicted.
- **C3.** The classifying invariant is constructed **without a privileged interface** — e.g., defined by a universal property (limit/colimit over the interface category) rather than by choosing a maximal observer — or, if a privileged interface is used, a proof that the classification is independent of which admissible privileged interface is chosen.
- **C4.** Survival of adversarial completion: R's advocate (or a successor) is granted a preregistered, bounded adversarial budget to construct completions in 𝒞 attempting to flip a label, and fails, with the exhaustion protocol declared before the attempt.

If C1–C4 hold, R concedes the separation is well-formed and completion-stable *for class 𝒞*, and retreats to the class-relative remark that 𝒞-relativity is itself a form of context-relativity. R's advocate acknowledges this retreat position is substantially weaker and says so now, so that a C1–C4 success cannot later be relabeled a draw.

## R.5 What R is not (anti-strawman clauses)

- R is **not** verificationism about all discourse; it is operationalism about *capability attributions* specifically, where the concept's content demonstrably comes from test procedures.
- R is **not** the triviality "everything is relative": Rule R2 gives R a sharp internal distinction with empirical bite, and Rule R3's ill-formedness verdict has strict conditions.
- R is **not** committed to denying the *usefulness* of fixed-mechanism modeling; it denies that fixed-mechanism talk picks out an access-independent capability fact rather than a convenient completion choice.
- R does **not** predict all paired cases are unclassifiable; it predicts classification is completion-relative, which is compatible with high local stability.

## R.6 R's own falsification conditions (by R's best advocate)

R is falsified or forced into major revision if any of the following is established:

- **(a) Universal-property construction succeeds.** A capability quotient defined by categorical universality over interfaces (hence choice-free in the relevant sense) exists, is nontrivial, and classifies real paired cases — this defeats P1 at the root rather than instance-by-instance.
- **(b) Adversarial completion exhaustion.** Under a preregistered exhaustion protocol (bounded budget, declared search space, declared stopping rule), R's advocate cannot construct any admissible completion that flips any label on the preregistered case set — defeating P2 in the strongest fair form available to a universally quantified claim.
- **(c) Contextuality prediction fails.** For the studied case families, globally consistent access-subtracted capability assignments exist and restrict correctly to every context — the KS analogy demonstrably fails to bite where R said it would (P3).
- **(d) Self-application failure.** It is shown that R's own Rule R2 (conservative-extension test) cannot be stated without smuggling a privileged background interface — i.e., R's internal distinction has the same alleged defect as the rival's. R's advocate accepts this as a genuine symmetry-breaker against R, not a tu-quoque draw.

**What does NOT falsify R:** single cases where labels agree across the frames actually on the table (agreement among frames sharing a completion convention is predicted by R); classifications stable only under one preferred completion; or the mere formal definability of a normalized capability (definability is cheap; completion-stable definability is the test).

---

# DELIVERABLE 2 — TI SOURCE-SIDE PAIRED CASE (candidate packet content)

**Candidate packet ID:** `TI-PIT-02` (paired-intervention test, second source case)
**Status:** `DRAFT-FOR-SOURCE-ISSUANCE` — this is authored content proposed *to* the Temporal Issuance repository. It is not a source-issued frozen packet until the TI steward adopts, revises, pins, and issues it under a packet schema version. Under the frozen-packet rule, the receiver must treat this case as `NOT_YET_IMPORTABLE` until TI issues it.
**Proposed TI path (TI owns final placement):** `temporal-issuance/interfaces/possibility-to-capability/TI-PIT-02-proj-vs-adm-paired-intervention-v0.1.md`
**Source-sovereignty statement:** Temporal Issuance owns all definitions used here (`OnlineIssuance^LC` components per `FORMAL-OBJECT.md` RUN-0081), owns the fixture, its evidence, and its claim status. Nothing in this packet moves, reopens, or reinterprets any TI claim (`TI-C019` and `TI-C020` status untouched; `Issue[S]^physical` remains `false`; the fixture below is a finite toy instance, not evidence about physical issuance). The receiver may classify; it may not upgrade.

## 2.1 Exact small objects

A finite instance of the `OnlineIssuance^LC` signature (Gamma_n, Adm_n, CandExt, Iss_n, Proj_{o,n}, tau_n); Glue_n is not exercised (single observer).

- **Tokens:** binary strings over {a, b}. **Gamma_0 = {"a"}.**
- **CandExt(Gamma) = { t·c : t ∈ Gamma, c ∈ {a,b} }** (one-letter right-extensions of already-issued tokens; formed at the prefix — no token not derivable from the current context is a candidate).
- **Adm_k(e) ⇔ e ∉ Gamma ∧ |e| ≤ 4 ∧ #b(e) ≤ k** (admissibility parameter k).
- **Iss_n:** at stage n, issue *all* admissible candidates in sorted order; Gamma_{n+1} = Gamma_n ∪ issued. Deterministic. Run stages n = 1..4 (stage 4 is provably quiescent — checked below).
- **tau:** trace = the ordered list of (stage, token) issuance events.
- **Proj_L (observer projection):** the observer receives exactly the events whose token has length ≤ L. `Proj_full` = identity on the trace.
- **Shared task vocabulary V** (issuance/reconstruction tasks over Voc = all 30 binary strings of length 1–4):
  - **ISS(s):** "is s issued by stage 4?" — a source-side issuance fact.
  - **REC(s):** "does the observer's projected record *determine* ISS(s)?" — a reconstruction fact. Reconstruction model (declared, load-bearing): the observer knows Gamma_0, CandExt, Iss, the stage count, the projection rule, and that Adm = Adm_k for some k in the hypothesis class **H = {0,1,2,3}**; the observer does not know k. REC(s) is decided iff every k ∈ H whose projected trace equals the observed one agrees on ISS(s).

## 2.2 The paired intervention

- **BASE:** k = 1, projection Proj_{L=2}.
- **ALPHA (projection-side intervention):** change *only* Proj: Proj_{L=2} → Proj_full. Issuance structure (Gamma_0, CandExt, Adm_1, Iss) bit-identical to BASE.
- **BETA (admissibility-side intervention):** change *only* Adm: Adm_1 → Adm_2, so candidate extensions containing two b's become issuable. Projection bit-identical to BASE (Proj_{L=2}).
- **Supplementary fixed-projection pair (BASE′/BETA′):** same two systems under the fixed wider projection Proj_{L=3}, included so the record carries a regime where the admissibility intervention *is* observer-visible (visibility of BETA under Proj_{L=2} is a parameter fact, not a structural necessity).

No leg is labeled ACCESS_CHANGE or CAPABILITY_CHANGE here. The packet reports computed before/after facts only; classification belongs to the receiver's preregistered gate run, with frames proposed before outcomes are classified.

## 2.3 Machine-checkable computation (embedded, complete)

```python
# TI-PIT-02 paired-intervention fixture (candidate, source-issuance pending)
from itertools import product

ALPH = "ab"; N_STAGES = 4; MAXLEN = 4; HYP = [0, 1, 2, 3]

def run_system(k):
    gamma = {"a"}; trace = []
    for n in range(1, N_STAGES + 1):
        cands = sorted({t + c for t in gamma for c in ALPH})
        issued = [e for e in cands
                  if e not in gamma and len(e) <= MAXLEN and e.count("b") <= k]
        trace.extend((n, e) for e in issued)
        gamma |= set(issued)
    return {e for (_, e) in trace}, trace

def project(trace, L):
    return list(trace) if L is None else [(n, e) for (n, e) in trace if len(e) <= L]

VOC = sorted(("".join(p) for l in range(1, MAXLEN + 1)
              for p in product(ALPH, repeat=l)), key=lambda s: (len(s), s))
SYS = {k: run_system(k) for k in HYP}

def analyze(true_k, L):
    obs = project(SYS[true_k][1], L)
    consistent = [k for k in HYP if project(SYS[k][1], L) == obs]
    decid = [s for s in VOC if len({s in SYS[k][0] for k in consistent}) == 1]
    return {"issued": sorted(SYS[true_k][0], key=lambda s: (len(s), s)),
            "proj_record": obs, "consistent_k": consistent, "decidable": decid}

CONDS = {"BASE  (k=1,L=2)": (1, 2), "ALPHA (k=1,full)": (1, None),
         "BETA  (k=2,L=2)": (2, 2), "BASE' (k=1,L=3)": (1, 3),
         "BETA' (k=2,L=3)": (2, 3)}
R = {name: analyze(*a) for name, a in CONDS.items()}
# (printing/delta/invariant-check code as run; full script preserved at
#  scratchpad ti_paired_case.py, to be inlined verbatim on TI issuance)
```

## 2.4 Computed output (verbatim from the run)

```text
== BASE   (k=1, Proj L=2)
   IssuedSet (9): ['aa','ab','aaa','aab','aba','aaaa','aaab','aaba','abaa']
   ProjRecord (2 events): [(1,'aa'),(1,'ab')]
   consistent k under HYP: [1, 2, 3]
   DecidableSet (25/30)

== ALPHA  (k=1, Proj full)
   IssuedSet (9): identical to BASE
   ProjRecord (9 events): full trace
   consistent k under HYP: [1]
   DecidableSet (30/30)

== BETA   (k=2, Proj L=2)
   IssuedSet (13): ['aa','ab','aaa','aab','aba','abb','aaaa','aaab','aaba',
                    'aabb','abaa','abab','abba']
   ProjRecord (2 events): [(1,'aa'),(1,'ab')]      <- identical to BASE
   consistent k under HYP: [1, 2, 3]
   DecidableSet (25/30)                            <- identical to BASE

== BASE'  (k=1, Proj L=3):  Issued 9;  ProjRecord 5 events; consistent [1];   Decidable 30/30
== BETA'  (k=2, Proj L=3):  Issued 13; ProjRecord 6 events; consistent [2,3]; Decidable 29/30

== Pairwise deltas (added, removed)
 BASE  -> ALPHA: issued +[] -[];  decidable +['abb','aabb','abab','abba','abbb'] -[];  projRecord identical: False
 BASE  -> BETA : issued +['abb','aabb','abab','abba'] -[];  decidable +[] -[];         projRecord identical: True
 BASE' -> BETA': issued +['abb','aabb','abab','abba'] -[];  decidable +[] -['abbb'];   projRecord identical: False

== Invariant checks
ALPHA leaves source issuance fixed: True
BETA  leaves L=2 projected record fixed: True
BETA  leaves L=2 decidable set fixed: True
BETA' changes L=3 projected record: True
Quiescence at stage 4 (no events, all k): True
```

**Structural summary of the dissociation (facts, not labels):**

- **ALPHA:** source issuance set exactly fixed (9 tokens, element-for-element); observer's decidable set grows 25 → 30; the five newly decidable tasks (`abb, aabb, abab, abba, abbb`) are all decided *negative* — tokens the fixed structure never issues.
- **BETA:** source issuance set grows 9 → 13 (four genuinely new issued extensions, each with witness `#b(e) = 2 ≤ 2`); the observer's projected record and entire decidable set are bit-for-bit unchanged — through the fixed L=2 projection, BETA is invisible.
- **BETA′ (fixed wider projection):** the same admissibility change is observer-visible (`abb` enters the record at stage 2) and *shrinks* reconstruction decidability by one task (`abbb` becomes undecidable: consistent hypotheses {2,3} disagree on it) while enlarging issuance. An admissibility-side intervention can simultaneously enlarge what is issued and reduce what is reconstructible.

## 2.5 Assumptions (all load-bearing, declared)

1. Finite toy instance; MAXLEN = 4, four stages, quiescence machine-checked. No claim of generality beyond the instance.
2. The reconstruction model's hypothesis class H = {0,1,2,3} is a *choice*; REC-facts are relative to H. A different declared H changes decidable sets. (This is exactly the kind of declared free parameter Frame R's P1 predicts; the packet records the parameter without adjudicating.)
3. "Issuance structure fixed under ALPHA" and "projection fixed under BETA" are certified by identity of the defining code, i.e., by out-of-model access to the model text, not through any in-model interface (cf. Frame R Rule R4 — recorded as a fact about the certificate's provenance, not as a classification).
4. `Iss_n` issues all admissible candidates deterministically; single observer; `Glue_n` not exercised.
5. This instance sits below the `OnlineIssuance^LC` gate's clause 4 concerns (a fixed-law absorber trivially covers a finite deterministic toy); it is a *task-vocabulary and intervention-structure* fixture, not a source-issuance witness. It must not be cited as evidence for `Issue[S]` of any type.

## 2.6 Grades

- Fixture computation: **machine-checked** (deterministic script, run 2026-07-16, full output above); **not** proof-assistant verified.
- Design claims (that ALPHA/BETA constitute a well-formed paired intervention over a shared vocabulary): **exploration grade**.
- Physical or source-issuance significance: **none claimed** (`Issue[S]^physical: false` unchanged; TI-C019/TI-C020 untouched).
- Independence: authored in the firewalled rival lane without sight of receiver diagnostics; a receiving independence assessment is the receiver's to make and cannot strengthen anything stated here.

## 2.7 Open fields for the TI steward at issuance

Schema version to issue under (v0.1 vs v0.2 with bundle digests); whether to include the script as a manifest blob (recommended: yes, with `content_sha256`); whether TI wants a `Glue_n`-exercising variant (two observers, reconciliation) before issuance; final packet ID.

---

## Proposed repo paths (nothing written to any repo)

1. Frame R: `possibility-to-capability/packets/rival-frames/FRAME-R-access-constitutive-v0.1.md` (receiver decides final placement; I could not check receiver placement conventions without reading forbidden directories).
2. TI candidate packet: `temporal-issuance/interfaces/possibility-to-capability/TI-PIT-02-proj-vs-adm-paired-intervention-v0.1.md` (TI owns final placement and issuance).
3. Script (currently at scratchpad `ti_paired_case.py`): to be inlined verbatim as a manifest blob in the issued packet.

## Files consulted (complete declaration for referee audit)

- `possibility-to-capability/governance/CHARTER.md` — read in full.
- `possibility-to-capability/experiments/2026-07-14-ranked-decisive-test-program-v0.1.md` — heading list only (grep for `^#` lines, to locate section boundaries), then lines 104–169 (the Rank-2 section) only. No other section read.
- `possibility-to-capability/packets/README.md` — read in full.
- `possibility-to-capability/packets/schema/README.md` — read in full.
- `possibility-to-capability/packets/schema/frozen-packet-v0.2.md` — read in full.
- `temporal-issuance/FORMAL-OBJECT.md` — read in full.
- No file under `explorations/`, `synthesis/`, `tests/`, no other `experiments/` content, and no file matching `R2`, `access-capability`, `pilot`, `decisive-tests`, `fiber`, or `lane` was read, listed into context beyond the schema directory filename glob above, or delegated to any agent. The `packets/schema/` glob returned five filenames; two schema JSON files (`frozen-packet-v0.1.schema.json`, `frozen-packet-v0.2.schema.json`, `receiving-independence-assessment-v0.2.schema.json`) were not opened, as the normative `.md` contract sufficed.
- Written outside any repo: `C:\Users\joe\AppData\Local\Temp\claude\C--Users-joe-JB\38a54106-9cc3-428a-92e4-2bb753410f40\scratchpad\ti_paired_case.py` (executed once; output embedded above).

---

# ATTACHED ADVERSARIAL REFEREE REPORT (governs)

# REFEREE REPORT — Lane C-frame-r-and-second-case (Frame R v0.1 + TI-PIT-02 draft)

**Referee re-execution receipts (2026-07-16):**
- Scratchpad script `ti_paired_case.py` exists (4,042 bytes, 08:27) and was re-run: all numeric content reproduces — issued sets (9/13), projection records (2/9/2/5/6 events), consistent-k sets ([1,2,3]/[1]/[1,2,3]/[1]/[2,3]), decidable counts (25/30/25/30/29), all three pairwise deltas, all five invariant checks True, quiescence at stage 4 True.
- Independent hand re-derivation of the k=1 and k=2 issuance cascades, the L=2/L=3 hypothesis-consistency sets, and the 5-task undecidable set {abb, aabb, abab, abba, abbb} — all match.
- TI sovereignty cross-check against `temporal-issuance/FORMAL-OBJECT.md`: RUN-0081 exists; TI-C019 target, TI-C020 parked, `Issue[S]^physical: false`, and the clause-4 absorber list ("finite/computable/fixed-law … absorbed") all match the lane's statements. The lane correctly self-classifies its toy as absorbed, not a source-issuance witness.
- ADAPTER2-01 checked at its governing statement (Bounded Fiber Theorem v1.0): it withdraws `bar(b) = finality-axis polarity` and caps the tri-repo adapter at common-predicate grade. The lane touches neither `bar(b)`, polarity, nor the adapter — no contradiction.
- Files-consulted audit: program heading map confirms Rank 2 spans lines 104–169 exactly (Rank 3 heading is line 170); `packets/schema/` glob returns exactly five files as declared.

## (1) VERDICT: **SOUND-WITH-CORRECTIONS**

All computations verify on independent re-run; source sovereignty is respected; no receiver verdict, gate run, or update-vocabulary movement occurs; the packet correctly self-marks `DRAFT-FOR-SOURCE-ISSUANCE` / `NOT_YET_IMPORTABLE`. But the frame–case pairing carries an undeclared mutual-contamination structure with false "in advance" prediction language, the "verbatim" receipt claim is false in form, and the fixture is read one-sidedly in Frame R's favor against R's own falsifier (c).

## (2) Defects

**D1 — MAJOR — Co-authored frame and case cross-cite each other; P4 claims prediction credit it cannot have; the candidate source packet is frame-spoiled as drafted.**
Frame R's P4 says "R flags **in advance**: the TI computation below exhibits exactly this," then "R's thesis, exhibited rather than refuted." "In advance" is false: frame and case were authored by the same lane in the same pass; on TI-PIT-02, P4 is postdiction with a cannot-fail structure (the author built the case that "confirms" the prediction). Symmetrically, the candidate **source** packet cites the rival frame inside its own assumptions (assumption 2: "exactly the kind of declared free parameter Frame R's P1 predicts"; assumption 3: "cf. Frame R Rule R4"). The program requires issuers blind to diagnostic apparatus; a source packet textually coupled to one contending frame is spoiled in exactly the pre-labeling sense, merely with rival-frame labels instead of receiver labels. The lane's §2.6 independence line addresses receiver-independence only; frame↔case mutual dependence is never declared. (Producing both artifacts was the lane's assignment; the defect is the undeclared coupling and the "in advance" credit claim.)
*Corrected wording:* (i) P4: "the co-authored TI-PIT-02 fixture exhibits this structure; because frame and case share an author, R earns **zero** predictive credit from it — P4 is testable only on cases R's advocate did not author." (ii) Strip all Frame-R citations from the packet text before TI handoff; state the provenance facts (out-of-model certification of the "held fixed" legs) in frame-neutral language. (iii) Add to both deliverables: "Frame R and TI-PIT-02 may not be used together as a frame-discriminating pair in any Rank-2 run; the program's independent-reviewer and blind-issuer requirements must each be satisfied by parties other than this lane."

**D2 — MODERATE — "Verbatim from the run" is not verbatim; the embedded code is not the code that ran.**
The actual script output prints full decidable-set lists, multi-line pairwise deltas, and condition headers `BASE   (k=1, Proj L=2)`; the report's output block suppresses the lists, compresses BASE′/BETA′ to single lines, rewrites delta lines with semicolons, and the embedded `CONDS` keys (`"BASE  (k=1,L=2)"`) match neither the scratchpad script nor the report's own output headers. Content is faithful (referee re-run confirms every number), but a hand-reformatted block labeled "verbatim," produced by code partially elided from the deliverable, is precisely the unmechanized-receipt pattern this swing exists to end — and lane A's mechanism standard applies to this lane too. The scratchpad copy is session-ephemeral, not a receipt.
*Corrected wording:* inline the full script (as §2.7 already recommends for issuance), replace the output block with the true byte-level output, and label any abridgment "condensed by the author; full output reproducible from the inlined script."

**D3 — MODERATE — The fixture is an instance of Frame R's own falsification regime (R.6c), presented only as support (P4).**
The TI family is deterministic and single-observer; the true k lies in every consistent hypothesis set, so every context-decided REC-fact is a restriction of the single global ISS profile. A globally consistent, access-subtracted assignment exists **by construction** and restricts correctly to every declared interface — the exact "globally patchable" configuration R.6(c) names as evidence against R's contextuality prediction P3. The deliverable presents the case as "R's thesis, exhibited" and never notes that this family can never exhibit a KS-style obstruction. One-sided reading in the rival's favor.
*Corrected wording:* "This family is globally patchable by construction (compatible contexts, definable global truth); it provides no support for P3 and, if such families exhaust the receiver's case domain, feeds R.6(c) against R. P3 requires families with incompatible interface contexts — e.g., a `Glue_n`-exercising multi-observer variant (§2.7)."

**D4 — MINOR — RUN-0081 signature misquoted.**
The source tuple is `(Gamma_n, Adm_n, Ext_n, Iss_n, Proj_{o,n}, Glue_n, tau_n)`; the lane writes `(Gamma_n, Adm_n, CandExt, Iss_n, Proj_{o,n}, tau_n)` — `CandExt` belongs to the gate clauses, not the signature slot; the witness-bearing `Ext_n` is silently dropped, and gate clause 2's `w_n : Adm_n(e_n)` witnesses appear only in prose ("witness #b(e)=2≤2"), never as fixture objects.
*Corrected wording:* quote the seven-component signature exactly, state which components are instantiated/trivialized (`Ext_n` collapsed to `CandExt` with inline witnesses; `Glue_n` not exercised), and carry `w_n` as an explicit field in the trace events before TI issuance.

**D5 — MINOR — Cannot-fail invariant check presented as evidence.**
"ALPHA leaves source issuance fixed: True" compares `SYS[1][0]` with itself (the issued set is a function of k alone; both legs use k=1) — tautological, cannot fail under any bug. It is listed undifferentiated among four substantive checks and re-used in §2.4 as "exactly fixed, element-for-element." The fixture also carries no failing-direction controls at all (excusable pre-issuance: the house [T]/[E]/[F] discipline lives under forbidden `tests/`, unreadable through the firewall — but required before issuance).
*Corrected wording:* tag the ALPHA-invariance line "holds by construction of the fixture (theorem-consequence, no evidential weight)"; add failing-direction probes (e.g., perturbed k or truncated MAXLEN flipping the substantive checks) when inlining for TI.

**D6 — MINOR — Files-consulted declaration arithmetic.** "two schema JSON files" followed by three filenames (`frozen-packet-v0.1.schema.json`, `frozen-packet-v0.2.schema.json`, `receiving-independence-assessment-v0.2.schema.json`). The five-file glob claim is correct; the slip is trivial but sits in the audit-bearing declaration. Correct "two" → "three."

**D7 — MINOR — "stage 4 is provably quiescent — checked below."** It is machine-checked on the instance; no proof is written (one is trivial from the MAXLEN bound). "provably quiescent" → "machine-checked quiescent (trivially provable from the length bound; proof not written)."

**D8 — ADVISORY — Proposed placement.** `packets/rival-frames/` sits under a directory `packets/README.md` reserves for immutable manifests, provenance receipts, and analyses of frozen **imports**; a receiver-authored rival frame is none of these. The lane properly deferred placement; the receiver should route Frame R to `experiments/` or a new rivals area, and (per D1) quarantine it from the eventual independent reviewer's authorship.

**Checked and cleared:** all computed facts (referee re-run + hand re-derivation); TI sovereignty statement matches TI's actual ledger, including the clause-4 absorber self-classification; ADAPTER2-01 uncontradicted (no bar(b)/polarity/finality content); no receiver-fiat moves (issuance, placement, and classification all correctly deferred; `NOT_YET_IMPORTABLE` self-applied); no update-vocabulary misuse (no FAVORS_* issued); Rule R3's ill-formedness conditions are strict enough to avoid trivializing the test, and the "what does NOT falsify R" clause, though it shields R at single-case granularity, is honestly paired with four real falsifiers including the self-application symmetry-breaker R.6(d) — which correctly anticipates that R2's "channel-wise inclusion" inherits the same mechanism/channel decomposition parameter P1 attacks; anti-strawman clauses accurate; no north-star/byproduct inversion.

## (3) What the deliverable actually earns

Program vocabulary: Rank 2 remains **`BLOCKED`** at the source-packet prerequisite — unchanged, and the lane claims nothing else. Additionally earned, at the stated grades:

- **Frame R** earns standing as a candidate for the Rank-2 prerequisite "strongest access-constitutive construction," at philosophical-argument grade, **conditional on**: adoption/re-freezing by an advocate independent of any case it will be scored on (this lane cannot be that advocate for TI-PIT-02), and the D1 correction voiding P4 credit on the co-authored case. Its concession conditions C1–C4 and falsifiers R.6(a)–(d) are genuinely pre-committed and non-vacuous — a real improvement over the receiver's own single-author Frame R placeholder.
- **TI-PIT-02** earns draft status only: a machine-checked (referee re-run) fixture correctly offered to TI as `DRAFT-FOR-SOURCE-ISSUANCE`, importable by the receiver only after TI steward adoption, with D1 (strip frame citations), D4 (signature/witness fidelity), and D5 (controls) applied first. It carries zero physical or source-issuance significance, as it itself states.
- **No update — not even directional pressure — toward any rival or the candidate** is earned by the frame–case pair jointly: on TI-PIT-02, Frame R's P4 is postdiction (D1), and the case family structurally cannot test P3 (D3). The pair's only admissible future role is as *separate inputs* to a run whose frame-advocate and issuer are distinct parties.

## (4) Stop-condition compliance

**Respected with one declared-late gap.** No classification was performed on any leg (labels correctly withheld); no receiver verdict issued while the prerequisite is absent; no source claim moved (TI-C019/TI-C020/`Issue[S]^physical` all correctly stated and untouched); the frozen-packet rule honored via `NOT_YET_IMPORTABLE`; nothing written to any repo (verified: no such files exist; proposed paths only). The gap: the program's fan-out posture requires rival-forecast drafting and source-packet preparation to have **distinct owners**; this lane authored both by assignment and never flags the consequence (D1). Acceptable only as draft input with the D1 quarantine language added; without it, a future run that scores Frame R against TI-PIT-02 would violate the Rank-2 stop "normalization chosen after seeing the desired answer" in its mirror-image form (case chosen after seeing the frame's prediction).

## (5) FIREWALL VERDICT: **INDEPENDENT**

Evidence:

- **(a) Declaration audit — clean.** The program read is scoped to lines 104–169, which I verified is exactly the Rank-2 section (Rank 3's heading is line 170). Charter, `packets/README.md`, both schema `.md` files, and `temporal-issuance/FORMAL-OBJECT.md` are all outside the forbidden set. The schema-directory glob claim (five filenames) verified true. One arithmetic slip (D6) does not change scope. No forbidden path, name pattern, or delegated read appears.
- **(b) Vocabulary scan against `lane-R2-access-capability-pilot.md` — no tell-tales.** Every receiver-operational term is absent from the lane: `Δop`, `Δ(c)`, completion-ROBUST/RELATIVE, the access **lattice** and its "top," record-channel inventory, sealed intent label, evidence-functionality test, PREREG/expectation matrix, POMDP worlds, oracle action, "latent enlargement," the receiver's four case names, and all FAVORS_* forecast wordings. Shared terms all trace to permitted files: `ACCESS_CHANGE`/`CAPABILITY_CHANGE`, "no invariant quotient," "access-constitutive," and "no shared task comparison is meaningful" are quoted from the program's Rank-2 section (permitted); the "completion" family derives from the charter ("completion tests," "fixed-family completion," completion/null gate) and from TI's own absorber vocabulary the lane legitimately read ("external Platonist completion," "external completed-structure ontology") — of which lane C's "God's-eye task ontology" is the standard Putnam-idiom rendering. The two soft overlaps with the receiver's Frame R ("completion-stable," "God's-eye") are natural coinages on those permitted seeds, embedded in machinery wholly absent from the receiver corpus (interface triples, conservative extensions, Bridgman/RQM/Kochen–Specker layers).
- **(c) Positive independence signal.** P5 stakes an extensional-coincidence prediction that the receiver's *existing* pilot already pressures (the pilot's pure-capability case is a repertoire enlargement that is R2-conservative under the channel reading yet completion-robustly CAPABILITY_CHANGE under Frame N); a contaminated author would likely have hedged or avoided P5. The one structural convergence — a structure change invisible through the fixed projection, echoing the receiver's case-d signature — is forced by the paired-intervention design the program itself specifies, and the lane's realization (issuance/projection in TI's own signature) shares no mechanism with the receiver's POMDP construction.