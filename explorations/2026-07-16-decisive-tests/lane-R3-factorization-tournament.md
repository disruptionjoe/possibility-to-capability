> Part of the 2026-07-16 decisive-tests swing (north-star lane; ranked program v0.1, ranks 1-4). Exploration tier; receiver updates only; the attached referee report GOVERNS — its corrected receiver update supersedes the lane headline.

# RANK 3 MAPPING AND PROVISIONAL ADJUDICATION — fiber corpus as finality-factorization tournament

**Status of this output: PROVISIONAL / PREPARATION grade.** Rank 3's prerequisites are not all met (Rank 2 not run; program file itself is `status: proposed_not_authorized`), so per the program's serialization rule this adjudication is stated conditionally and must never be averaged into a pass. Exploration tier; no source claim moves; bar(b), H59, Krein positivity, physical issuance remain OPEN; ADAPTER2-01 governs throughout.

The finality candidate under test: GU-001's external Z/2 settlement datum — the sector sign, located per ADAPTER2-01 as an untransported fiber, with physics name "sector-asymmetric spectral condition" (Bounded Fiber Theorem link 11).

---

## 1. Mapping table: Rank 3's four required legs → existing artifacts

Rank 3 (program §Rank 3, Construction) requires, per candidate, preregistered independent teams/arguments on four legs. All grades below are the referee-corrected ones (each lane file's attached referee report governs).

| Rank 3 leg | Instantiating artifacts | Referee-corrected grade | Leg status |
|---|---|---|---|
| **(i) Strongest enlarged-dynamics / coarse-graining model** | `tests/condition2prime_constructive_probe.py` (three routes: irreversibility-as-spectrum, record-growth functionals, fork-realization; consumption point exhibited per attempt) — `explorations/2026-07-16-fiber-closure/lane-A-2prime-constructive.md`. Arrow⊥sector independence (link 10; "even a resolved T57 arrow cannot close 2′"). `tests/krein_norm_link_probe.py` (REDUCED: posit relocates to the spectral condition) — `explorations/2026-07-16-swing-2/lane-3-krein-normlink-probe.md`. Partitioned transport (ℤ/2)^blocks — `tests/gate1_enriched_toy_nonthin_multiobserver.py`. | Finite-toy executable certificates, negative-leaning; lane-A referee: FAILED-ALL-ATTEMPTS stands (growth-blindness corrected to strict-positivity scope). REDUCED at toy grade (lane-3 referee; orbit lemma tautological over toy functionals, unproven over richer frameworks). | **INSTANTIATED at toy-family grade, partial.** Three dynamics families searched; synthesis §4 explicitly: none-found ≠ none-exists over unsearched families; no continuum / infinite-dimensional Krein-QFT model attempted. |
| **(ii) Strongest boundary-condition or global-constraint model** | Closest fits: `tests/no_go_2prime_flip_equivariance.py` (constraint structure: every definable functional over the 36-symbol flip-automorphic vocabulary is flip-equivariant; 844-tuple battery) — `explorations/2026-07-16-fiber-closure/lane-B-2prime-nogo.md`. GU-001 gate 4 (completion-null: residual survives all DECLARED completions; "unbuilt completions explicitly untested") — `packets/imports/GU-001/GU-001-GR-001-gate-run-v0.1.json`. `tests/physical_boundary_adapter.py` + `tests/physical_completion_closure.py` declare "boundary" and "whole-family" completion kinds — but they target the capability adapter for a future physical witness, **not** the Z/2 sign datum. | Class-relative toy-grade proof over a **stipulated** vocabulary class (lane-B referee: NO-GO survives; "derivable"→"expressible" and L6-relocation corrections adopted). Gate 4: receiver-verified structural, source-side completions only. | **MISSING as such.** The constraint-structure *vocabulary* leg exists; a strongest physics-sense boundary-condition model (past-hypothesis-style asymmetric boundary datum, asymptotic/radiation condition, GU source-action boundary term) attempting to factor the sector selection was never built. The escape-door enumeration names this territory (door 4, the physical door: "all roads priced it; none walked it") but pricing is not a factorization attempt. |
| **(iii) Strongest records / access / capability factorization** | Value-blind collapse lemma + D1Field branch-preserving functor (`tests/gate1_branch_preserving_functor.py`, links 3–4) — `explorations/2026-07-16-big-swing/lane-B-gate1-functor.md`. Non-thin, multi-observer enriched lift (`tests/gate1_enriched_toy_nonthin_multiobserver.py`, link 5). Shipped T26 closure + binary classification lemma "label-free = flip-even" (`tests/gate1_t26_actual_patch_machinery.py`, link 6) — `explorations/2026-07-16-fiber-closure/lane-C-t26-actual-patch.md`. `tests/relational_dissolution_fixture.py` (every flip-invariant Boolean observable factors through relative alignment; n=3,4 exhaustive; anchor-smuggling controls) — `explorations/2026-07-16-relational-dissolution/SYNTHESIS.md`. Aliveness anchor failure as records/readout-side factorization attempt (`tests/aliveness_neutrality_test.py`, link 12; framework theorem). | Definitional lemma + exhaustive-finite on thin toys (swing-1 lane-B referee: demoted from disproof); exhaustive-finite enriched toys (swing-2 lane-2 referee); **shipped-code grade** for the driven T26 surface, HIDING-PLACE-CLOSED for the named surface (swing-3 lane-C referee, riders adopted); exhaustive finite toy; framework theorem on the toy vocabulary (swing-1 lane-D referee). | **STRONGEST leg — instantiated at multiple tiers.** Gaps carried honestly: binary alphabet is fixture-imposed on T26's unconstrained string field; scalar/vector projections and `compare_systems` at source-audit grade only; native TaF/TI formalism tier untested (escape 3). |
| **(iv) Strongest admissible-continuation reopening construction** | Enumerated escape doors, each priced (synthesis §3: doors 1, 2, 2a, 3, 4) + reopen conditions (§5) + ADAPTER2-01's six-condition reopen burden + link 7's per-attempt consumption points and none-found ≠ none-exists scoping. | Enumeration is honest, referee-checked, and priced; it is not itself a construction. | **PARTIAL.** A continuation *class* exists ex post; no strongest reopening was *constructed* (the relational-dissolution handoff names the next one: the weakest-oracle theorem — the minimal flip-odd primitive that breaks the quotient). The class was also not frozen before evaluation, which Rank 3's prerequisite demands. |

Ancillary prerequisite item: "a named settlement witness" — partially met; link 11 names the datum's physics home (Krein form sign ≡ η-norm monotonicity direction ≡ bounded-below-spectrum sector), which is a relocation, not a witness of settlement.

**Net:** the corpus is a genuine, adversarially-refereed factorization tournament for legs (i) and (iii), a partial one for (iv), and does **not** contain leg (ii) in the sense Rank 3 requires.

---

## 2. Discipline audit against Rank 3's own rules

**Preregistration: honestly NO.** What actually happened and what it does/does not substitute:

- *Substitutes (real but partial):* (a) the adversarial-twin design — lanes A (construct) and B (no-go) ran simultaneously with **pre-committed pair-consistency adjudication rules** in both referee reports, which limits verdict-shopping between the twin outcomes; (b) two-stage research→adversarial-referee protocol in every lane, with the referee independently re-executing fixtures (seven re-run exit 0 by the synthesis's own pre-commit referee); (c) [T]/[E]/[F] tagging with every [F] exercising its checker, mechanized by `tests/tef_check_tag_linter.py --strict`; (d) deterministic, runnable fixtures pinned to commits.
- *Not substituted:* temporal commitment **before outcome inspection**. Post-hoc refereeing verifies what was found; it cannot certify that the search space (the 36-symbol vocabulary, the three dynamics families, the binary alphabet) was not shaped by earlier results, nor that stopping was outcome-independent. The corpus itself concedes this (synthesis §4: "pre-declared expectations / first-run process claims are assertions, not mechanized receipts; no two-phase expectation commits exist yet"). GR-001's referee D2 documents exactly the burned class: a receipt describing an invocation that did not run as quoted.
- *Independence: NO.* All legs were authored by one agent lineage; Rank 3 asks for independent factorization teams. Refereeing was adversarial but same-session-architecture, not organizationally independent.

**Evaluator blinding: partial, axis-specific.** The flip-equivariance/neutrality machinery is a *mechanized* blind on the **branch axis**: verdicts are forced to be invariant under exact branch-label swap (GR-001 gate 8 NO_BRANCH_SELECTED; `tests/neutrality_executed_harness.py` with a vacuity guard that demonstrably has teeth; the lane-D aliveness failure proves the criterion can fail). On that axis it is arguably stronger than human blinding. But Rank 3's blinding is about **preferred branch labels of the tournament outcome** too, and there is no blind on the **outcome axis**: the evaluator knew throughout which result ("wall stands") favors the candidate. Mitigations: Failure-Preservation makes a found factor first-class (lane A genuinely built three factor attempts and the referee confirmed each consumption point), and the twin structure means either twin outcome had a committed home. Also note GR-001's own neutrality pass is at **recorded-metamorphic** grade, not an executed swap within the run (referee D3) — full executed neutrality is blocked on GU source-side label-parameterized entry points (routed to GU by mailbox).

**Borrowed legs across constructions: no violation at the currently claimed grade.** GR-001's No-Artificial-Success gate enforced one chain, one branch, mechanically. The Bounded Fiber Theorem is tier-indexed ("at every tier modeled") rather than claiming one construction spans all legs; the twin adjudication uses lane C's classification lemma as a consistency key, which is coherence-checking, not leg-borrowing. **However:** a full Rank 3 pass assembled from the existing artifacts *would* stitch three distinct constructions (binary fixture encoding on T26, the stipulated 36-symbol vocabulary, the toy dynamics families) into one tournament. That is admissible only if the frozen search space is declared ex ante as exactly that union; done implicitly or retroactively, it is the forbidden borrowing. This is a live hazard for anyone upgrading the corpus, not a defect of the corpus as scoped.

**Rank 3 stop conditions:** "irreversibility used as settlement" — actively honored, not merely avoided: route a1 tested irreversibility-as-spectrum and it failed to select, and link 10 proves the arrow cannot substitute for the sector bit. "Continuation class excludes an unwanted reopening without reason" — no exclusions found; every door is kept open and priced. "Factorization search is post hoc" — largely yes; this is the binding cap on grade.

---

## 3. Prerequisite check (honest)

| Rank 3 prerequisite | Status |
|---|---|
| Rank 1: accepted real packet + working source-receiver interface | **MET.** GU-001 imported at pin `77879e5`, receiver-verified byte provenance, 8/8 gate PASS at receiver-verified structural grade, exploration tier (`packets/imports/GU-001/`; swing-2 lane-1 referee: GO with five corrections, executed). Two honest qualifiers baked into that grade: neutrality pass recorded-metamorphic; finality pass definition-relative (owner = typed joint seam, disclosed). |
| Rank 2: access/capability stabilized OR construction fork retained explicitly | **NOT MET.** No paired intervention test exists; no two independently defended normalization frames; no frozen operational-context rival construction. Fragments only (GR-001 gate 5's basis-invariant capability distinction; the boundary-adapter/completion-closure access and resource controls). |
| Named settlement witness, factorization search space, admissible continuation class, rival forecasts frozen **before** evaluation | **NOT MET ex ante.** Witness partially named (spectral condition, link 11); search space declared tier-by-tier as work proceeded; continuation class enumerated after; rival forecasts absent. |
| (Program-level) tournament contract authorized | **NOT MET.** The program file is `status: proposed_not_authorized`. |

Consequence, per the serialization rule: no Rank 3 receiver verdict can be *issued*; only a preparation-grade conditional adjudication can be stated.

---

## 4. Provisional receiver update

**Formal serialized status: `BLOCKED`** — Rank 2 (a named prerequisite) is absent and the ex-ante freeze was never performed; downstream judgment is not run as a verdict.

**Preparation-grade conditional adjudication (what the evidence would fire, conditional on Rank 2 resolving and on the completion spec in §5):**

- **At full Rank 3 grade: `REMAINS_UNDERDETERMINED`.** No factor was found, but search coverage is weak *relative to Rank 3's own discipline*: one of four required legs (boundary-condition/global-constraint) is missing, no leg was preregistered, no team was independent, and outcome-axis blinding was absent. Per the program's menu this is exactly "no factor is found but search coverage is weak: finality remains a candidate only."
- **At the narrower declared scope** — toy families + shipped-T26 driven surface + the stipulated flip-automorphic vocabulary class, under adversarial-twin and referee governance substituting partially for preregistration — the evidence pattern matches the program's third outcome and would **provisionally favor the candidate need for a distinct finality diagnosis at that scope and no further**. The delta between this rider and an issuable `FAVORS_CANDIDATE` is precisely §5. This is a receiver update about receiver scope; it promotes nothing at any source.

**Trap check (program: "the finality criterion assumes the settlement it purports to establish → invalidate the test"): PASSED, with two disclosed relativizations, so the test is not invalidated.**

1. The machinery does not presuppose the Z/2 is external. The flip-automorphism of the no-go vocabulary is *certified* by the exhaustive 844-tuple battery, not assumed; a non-automorphic symbol is *identified* by the battery, not excluded by definition. Externality itself enters only as GU's source-owned Godel-independence claim, imported at exactly source strength (Annotation B / IA-001) — the receiver machinery tests *derivability within declared structure*, which is a different predicate.
2. The residual circularity risk is scoping, not assumption: if "finality/issuance-native" were covertly defined as "flip-symmetric," the no-go would be circular. The corpus avoids this by stipulating the class openly (link 8: "the class is a **stipulated** formalization") and pricing the exit (door 2/2a: a natural flip-odd primitive is *characterized*, not refuted — whether it is posit or physics "is nature's question, not the toys'"). Likewise the collapse-or-smuggle dichotomy is explicitly relative to the σ-invariance criterion (link 3). These relativizations cap the grade at scoped; they do not beg the settlement.

---

## 5. Completion spec: what would earn a full-grade Rank 3 adjudication

1. **Authorize the tournament contract** (the program is `proposed_not_authorized`) and **resolve Rank 2** (run the paired intervention test, or retain its construction fork explicitly) — hard prerequisites.
2. **Ex-ante freeze, mechanized.** Before any new evaluation, commit: the settlement witness (the sector Z/2 / spectral condition per ADAPTER2-01), the factorization search space *declared as the explicit union of tiers* (discharging the borrowed-legs hazard in §2), the admissible continuation class, and rival forecasts. Implement the two-phase expectation-commit receipts the corpus already ranks as pending work (expectations committed to git first, results second).
3. **Build the missing leg (ii).** A strongest boundary-condition/global-constraint factorization attempt in the physics sense: a preregistered model where an asymmetric global/asymptotic boundary datum (past-hypothesis-style, radiation-condition-style, or a GU source-action boundary term) tries to select the sector; run it through the same battery/consumption-point discipline as lane A's three routes. Distinct from, and complementary to, the vocabulary no-go. The completion-closure family (`tests/physical_completion_closure.py`, boundary/whole-family kinds) should be retargeted from the capability adapter to the sign datum as a starting scaffold.
4. **Build leg (iv) as a construction.** Execute the weakest-oracle theorem (minimal flip-odd primitive breaking the relational quotient — the relational-dissolution handoff's rank 1) and the cyclic-orientation / |V| ≥ 3 fixture (door 1), plus the native-tier battery (door 3: TaF primitives beyond shipped T26; TI `OnlineIssuance^LC` at formalism grade; the finite-presentation rule of door 2a).
5. **Independent teams.** At least one leg authored by a lineage independent of the fiber-corpus authors, with source packets issued blind to receiver diagnostic labels (mirroring Rank 2's issuer-blindness rule).
6. **Blinded evaluator protocol on both axes.** Keep the mechanized branch-axis blind; add outcome-axis masking: adjudication rules and the outcome menu committed before results; where feasible, evaluator sees legs with branch labels randomized and without knowing which verdict favors which rival. Upgrade GR-001-class neutrality from recorded-metamorphic to executed, which requires the GU source-side label-parameterized entry points already requested by mailbox (fiber-closure lane D finding).
7. **Stop-condition audit as receipts:** demonstrate per leg that the continuation class excludes nothing without stated reason and that irreversibility is never used as settlement (the corpus already has the substantive results; convert them into per-leg receipts).

Even fully completed, the outcome remains scoped: `NO_FACTOR_FOUND` "is always scoped to the declared search; it never becomes absolute proof," and door 4 — whether nature supplies the flip-odd primitive — belongs to physics, not to this repository.

---

**Key paths:** program `experiments/2026-07-14-ranked-decisive-test-program-v0.1.md`; corpus spine `synthesis/2026-07-16-bounded-fiber-theorem-v1.0.md`; packet `packets/imports/GU-001/`; lanes under `explorations/2026-07-16-{big-swing,swing-2,fiber-closure,relational-dissolution}/`; fixtures under `tests/` (all repo-relative to `./`). Governing upstream correction: `../gu-formalization/explorations/time-as-finality-crosswalk/adapter2-correction-polarity-fiber-not-axis-2026-07-15.md`.

---

# ATTACHED ADVERSARIAL REFEREE REPORT (governs; corrected receiver update supersedes)

# REFEREE REPORT — LANE R3-finality-factorization-tournament

## Re-execution receipts

All 11 cited fixtures re-run from the working tree, all exit 0 with outputs matching the quoted content (`condition2prime_constructive_probe`, `no_go_2prime_flip_equivariance`, `gate1_branch_preserving_functor`, `gate1_enriched_toy_nonthin_multiobserver`, `gate1_t26_actual_patch_machinery`, `relational_dissolution_fixture`, `aliveness_neutrality_test`, `krein_norm_link_probe`, `neutrality_executed_harness`, `physical_boundary_adapter`, `physical_completion_closure`). `tef_check_tag_linter.py --strict` clean on the four headline fixtures (0 violations, [T]s excluded from headline, F-controls present). Verified against artifacts: GR-001 is 8/8 PASS at `evaluation_status: provisional` with pins d62a82f3/77879e5 as quoted; the 844-tuple battery and 36-symbol vocabulary counts are real; the swing-2 lane-1 referee's GO-with-five-corrections, D2 (receipt quoting an invocation that had not run as quoted) and D3 (neutrality is recorded-metamorphic, not executed) are quoted accurately; lane-A's FAILED-ALL-ATTEMPTS with the strict-positivity correction, lane-B's "derivable→expressible"/L6-relocation, lane-C's HIDING-PLACE-CLOSED riders, lane-3's REDUCED, and lane-D's FAILS-NEUTRALITY all match their governing referee reports verbatim in substance; the relational-dissolution n=3/n=4 exhaustive claim, anchor controls, and weakest-oracle rank-1 handoff are as stated; ADAPTER2-01's withdrawn identity is nowhere reasserted. The program file is confirmed `status: proposed_not_authorized` and its outcome menus read as the lane quotes them.

## (1) VERDICT: **SOUND-WITH-CORRECTIONS**

The mapping table, prerequisite check, formal `BLOCKED` status, missing-leg-(ii) admission, borrowed-legs hazard flag, and preregistration/independence confessions are honest and artifact-backed. One defect is major.

## (2) Defects

**R1 (MAJOR) — The narrower-scope conditional rider exceeds the program's outcome menu: the third-outcome antecedent is unfulfillable for the existing corpus.** §4 bullet 2 says the narrower-scope evidence "matches the program's third outcome and would provisionally favor the candidate need for a distinct finality diagnosis." The third outcome's antecedent is conjunctive: "no factor is found under **broad, independent, preregistered** searches and **no admissible continuation reopens the case**." The report itself concedes all four conjuncts fail at *every* scope: post hoc (its own §2: "the binding cap"), single lineage (independence: NO), no ex-ante freeze, and leg (iv) never *built* — an untested continuation class cannot count as "no continuation reopens." The program provides no substitution clause; "adversarial-twin + referee governance substituting partially for preregistration" is a category the menu does not contain. Worse, the conditionality is incoherent: §5's own item 2 requires the freeze "before any new evaluation," so no completion of §5 can ever retroactively make the *existing* searches preregistered or independent — the stated condition can never fire for this corpus. The rider is therefore a scoped partial pass over a conceded post-hoc search, which is precisely the class Rank 3's stop condition exists to stop, and it is the exact "synthetic/toy results creeping toward a receiver verdict" pattern. **Corrected wording:** "At every scope, including the narrower declared scope, the existing corpus fires only the program's second outcome: `REMAINS_UNDERDETERMINED`; finality remains a candidate only. Preregistration and independence are permanently absent from evidence already generated; the third outcome is reachable only by NEW evaluations run under §5. The twin/referee discipline raises confidence in the internal soundness of the no-factor findings at their declared scopes; it does not change the outcome class." The sentence "The delta between this rider and an issuable `FAVORS_CANDIDATE` is precisely §5" must be deleted: the delta for this corpus is not completable work but a fresh corpus.

**R2 (MODERATE) — The tournament candidate is receiver-designated, not source-issued; this is an unlisted prerequisite failure.** Rank 3: "TaF or another proposing source owns its finality candidate." GU-001's source claim is the Gödel-independence of `bar(b)` (W211, Located-Not-Forced); GU never issued the Z/2 *as a finality/settlement candidate for a factorization tournament*. Calling it "GU-001's external Z/2 **settlement datum**" quietly assigns it the settlement role — the very identification (sign = finality polarity) that ADAPTER2-01 withdrew and whose bridge is OPEN. The fiber location is correctly disclosed, so this is not an ADAPTER2-01 contradiction, but "source-issued finality candidate exists" belongs in the §3 prerequisite table as **NOT MET**, and §5 item 1 must add: obtain source issuance (GU or TaF mailbox) of the sign *as* a finality candidate, or re-scope the tournament to a candidate a source actually proposes. **Corrected wording (header):** "The receiver-designated candidate, derived from GU-001's source-owned independence claim; whether this datum is a *settlement* at all is part of the OPEN ADAPTER2-01 bridge, and source issuance of it as a finality candidate is itself a missing Rank 3 prerequisite."

**R3 (MINOR) — "Pre-committed pair-consistency adjudication rules" overstates.** Both referees' adjudication rules (lane-A §5, lane-B §5) were written *after* each referee had seen its own lane's outcome; they are pre-committed only relative to the joint synthesis adjudication. The report's "Not substituted" paragraph already concedes the general point, but the substitutes list should read: "adjudication rules committed before the joint twin adjudication, though after each lane's own outcome existed."

**R4 (MINOR) — Leg (i) mapping stretches `krein_norm_link_probe` under "enlarged-dynamics."** The probe is a relocation result (where the posit lives — witness-naming) rather than a dynamics model attempting factorization; listing it inflates leg (i)'s apparent coverage. The leg's "partial" grade already absorbs this; move the probe to the ancillary witness row.

**R5 (AWARENESS, not a defect of the report as written)** — the repo HEAD now contains post-corpus evidence (`tests/rank4_ternary_alphabet_representation_probe.py`, commit `5a21f3b`: "diagnosis stable, **lemma representation-dependent**") bearing directly on leg (iii)'s adjudication key (the binary-alphabet classification lemma). Any upgrade of this lane must carry that rider on the "STRONGEST leg" characterization.

Hunted and NOT found: [T]-checks counted as evidence (census discipline holds; lane-A referee verified the headline rests on proofs + [E]/[F], and the linter confirms exclusion); [F]-controls without teeth (f3/f3b/f4, a3-5 all exercise failing directions); borrowed legs at the currently claimed grade (the report's own hazard flag is correct and sufficient); representation-equivalence by receiver fiat (link 2 is disclosed as encoding-level, not native isomorphism); source claim movement (bar(b), H59, Krein positivity, physical issuance OPEN everywhere); ADAPTER2-01 contradiction (fiber-not-axis honored throughout, subject to R2's vocabulary caveat); fabricated referee quotes (all spot-checked verbatim-in-substance); strawmanning of the access-constitutive rival (the report does not adjudicate against it — it correctly reports Rank 2 absent and rival forecasts missing).

## (3) Receiver update the evidence ACTUALLY earns

**`BLOCKED`** — and nothing conditional beyond outcome 2. In full: Rank 3 prerequisites absent (Rank 2 not run; no ex-ante freeze; program contract `proposed_not_authorized`; **plus R2: no source-issued finality candidate**), so no Rank 3 verdict is issued. The only defensible preparation-grade conditional statement is: *conditional on all prerequisites resolving, the existing corpus — at any scope — fires `REMAINS_UNDERDETERMINED` ("no factor is found but search coverage is weak: finality remains a candidate only"), because the coverage-weakness clause is evaluated against Rank 3's own discipline (preregistration, independence, four legs, outcome-axis blinding), which the existing evidence permanently lacks.* The third outcome is reachable only by a new §5-compliant evaluation campaign. This is a receiver update; it promotes nothing at any source. The trap check ("criterion assumes the settlement") is legitimately PASSED-with-disclosed-relativizations — the stipulated-class and σ-relative disclosures are real in the artifacts — so the test is not invalidated.

## (4) Stop conditions

**Respected, with one strain.** Program-level serialization: respected (formal status is `BLOCKED`; no averaging into a pass; no parallel partial pass claimed to satisfy Rank 3). Rank 3 "irreversibility used as settlement": actively honored (route a1 + link 10, verified in re-run). "Continuation class excludes an unwanted reopening without reason": no exclusions found; doors open and priced. "Stop any claimed pass if the factorization search is post hoc": the report names this as its binding cap and issues no pass — **but the §4 narrower-scope rider (R1) is a conditional partial pass over that conceded post-hoc search and must be rewritten as above to keep the lane inside this stop condition.** With R1's correction applied, the lane is fully compliant.