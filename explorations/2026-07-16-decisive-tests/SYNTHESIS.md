---
artifact_type: exploration
status: complete
governance_role: swing_synthesis
constitutional: false
created: 2026-07-16
---

# 2026-07-16 decisive-tests swing — synthesis (referee-corrected receiver updates)

The north-star lane's first dedicated swing: ranks 1-4 of the ranked decisive-test program v0.1, four
lanes, each adversarially refereed (all four: SOUND-WITH-CORRECTIONS; the referees' corrected receiver
updates GOVERN and supersede the lane headlines). Receiver updates only; no source claim moves;
bar(b)/H59/Krein positivity/physical issuance OPEN; ADAPTER2-01 respected everywhere (each referee checked).

## Scorecard (program vocabulary, referee-corrected)

| Rank | Corrected receiver update | The real content |
|---|---|---|
| **1 — founding intake** | **FAVORS_CANDIDATE, conditional and scoped to the interface-formation discrimination only; Rank-1 discharge itself BLOCKED at prerequisite (v).** | The rival prediction "the interface cannot form a real joint object from a real source packet" took a referee-verified hit: gates/evaluator/stop conditions were committed ~17h before the source bundle existed, and (referee correction D3, cutting FOR the record) annotations A/B/C + pin were committed BEFORE any gate outcome. The un-preregistered discretion narrows to: the Gate-6 ownership construal (load-bearing), chain/branch ids, Annotation E, neutrality-artifact choice. Repairs: fully preregistered re-run on the next source packet + run the transition-diagnosis contract before any non-provisional update (a skipped serialization step the referee caught). Referee also caught the lane FABRICATING a program rule (a "provisional tier" the program lacks — the program's own word is BLOCKED); the conditional posture is receiver conservatism, owned as such. |
| **2 — access-vs-capability** | **BLOCKED at the source-packet prerequisite; receiver preparation discharged at synthetic-toy grade; NO directional pressure earned by the pilot itself.** | Real deliverables: an executable comparison contract (shared vocabulary, declared completion class, frames as pure functions, mechanical classification) with genuine failing directions; conditional forecasts admissible after corrections. Referee kills: the stop-condition "detector" was vacuous (constant-True — the burned class again); the rival was mis-scored against its own scope (its prediction is hedged to realistic interventions, untestable on designed-pure synthetic pairs); one contested disagreement was resolved by receiver fiat ("only on the WORD" — the Rank-4-banned move); prereg-named constants must be renamed EXPECTED_* before commit; the receiver-authored Frame R must be quarantined so it cannot anchor the future independent reviewer. |
| **3 — finality factorization** | **BLOCKED (prerequisites absent: Rank 2 unrun; no ex-ante freeze; program contract proposed_not_authorized; no source-issued finality candidate).** Only defensible conditional: under Rank 3's own discipline the existing fiber corpus fires **REMAINS_UNDERDETERMINED** ("no factor found but search coverage is weak") — because coverage-weakness is judged against preregistration/independence/blinding the corpus permanently lacks. The stronger third outcome needs a new §5-compliant campaign. | The mapping table (fiber corpus → the four tournament legs) is real preparation; the trap check ("does the criterion assume the settlement?") legitimately PASSES with disclosed relativizations. The corpus is evidence-shaped for the tournament but cannot be retroactively upgraded into one — adversarial-twin + referee structure substitutes for *some* of preregistration's function, not all, and the difference is now exactly characterized. |
| **4 — representation invariance** | **BLOCKED at Rank 4 (prereqs absent per serialization). Beneath the block, two unconditional preparation results:** (1) **the binary lemma BREAKS at \|V\|=3, machine-receipted** — {label-free} = {flip-even} fails at three letters (pattern classes split into multiple flip orbits), converting door 1's strictness from prose to receipt; (2) bijection-embedded-flip re-encodings are **definitionally non-discriminating** for Rank 4's question — that transformation family is excluded from future Rank 4 tests. Fork inventory retained; no forks averaged. | The \|V\|=3 receipt is the swing's genuinely new mathematics: it makes the cyclic-orientation fixture (Lane 1 item 3) *more* live, since the binary classification provably does not extend, and the larger-alphabet door is now open at receipt grade rather than caveat grade. |

## Program-level reading (honest)

The program's own 07-14 self-assessment said: "every current positive result is synthetic and the one real
intake is blocked." After this swing: **the one real intake happened and discriminated** (Rank 1's scoped
conditional FAVORS_CANDIDATE — the first non-synthetic evidential hit the hierarchy has taken), and every
other rank is BLOCKED for exactly the reasons the program predicted, now with executable receiver tooling,
characterized gaps, and named repairs instead of vague blockage. That is the north-star lane advancing the
honest way: one real hit, three precise blockades, two new receipts.

## What must be true next (repairs, in dependency order)

1. **Transition-diagnosis run on GU-001-GR-001** (`classify_transition.py`) — the skipped serialization
   step; cheap; blocks any non-provisional Rank-1 update.
2. **Two-phase preregistration mechanism** (expectations commit before results commit) — three referees
   across two swings have now flagged its absence; it gates Ranks 1-4 alike. (Lane-1 housekeeping item 5
   is the same work — converging demand.)
3. **Source-issued paired cases for Rank 2** (mailbox requests to TI/TaF or another source) + an
   independently authored Frame R (quarantine the receiver placeholder).
4. **Rank 3 §5-compliant campaign** only after 1-3.

## Process ledger

Fourth consecutive swing where referees found real defects in every lane — including a fabricated program
rule (R1), a vacuous stop-condition detector (R2, the burned cannot-fail class again), a receiver-fiat
resolution of a contested disagreement (R2), and a conditional-partial-pass rider over a conceded post-hoc
search (R3). All corrected in the governing updates. The two-stage structure stays mandatory; the
two-phase preregistration mechanism is now the highest-leverage process upgrade available.
