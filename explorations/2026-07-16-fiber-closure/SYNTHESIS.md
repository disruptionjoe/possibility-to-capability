---
artifact_type: exploration
status: complete
governance_role: swing_synthesis
constitutional: false
created: 2026-07-16
---

# 2026-07-16 fiber-closure swing — synthesis and twin adjudication (referee-corrected grades)

Four lanes: the condition-2′ **adversarial twins** (construct vs no-go), the T26 last-hiding-place fixture,
and the gate-tooling rider. All four referee verdicts: SOUND-WITH-CORRECTIONS; the attached referee reports
**govern the grades**. Exploration tier; no source claim moves; `bar(b)`, H59, Krein positivity, physical
issuance all OPEN; ADAPTER2-01 respected by every lane (each referee checked; lane B's conclusion in fact
independently matches the correction's "the finality order does not choose the fiber point").

## Twin adjudication (the swing's designed centerpiece)

Both referees supplied explicit pair-consistency rules; the outcome is the **coherent case** in both:

- **Lane A (construct): FAILED-ALL-ATTEMPTS, characterized.** All three constructions (irreversibility-as-
  spectrum, record-growth functionals, fork-realization) satisfy source-side + composition-compatible, and
  every one delivers **only the flip-orbit of a sector, never a member** — with the consumption point
  exhibited per attempt (a1: the source is blind to S-twins because S acts on the target only; a2: the bit
  enters as coupling-sign × channel-alignment; a3: realized-branch is per-history relabeling, D3-trap
  confirmed executably).
- **Lane B (no-go): PROVED, class-relative.** Over an explicit 36-symbol finality/issuance-flavored
  vocabulary certified flip-automorphic by an exhaustive 844-tuple battery, the automorphism-invariance
  theorem yields: every definable functional is flip-equivariant; no closed term names a sector; the
  **unoriented** sector-asymmetry is *expressible* (not derivable — referee B2) while its **orientation** is
  derivable only through a battery-identifiable non-equivariant symbol — **which is the posit.** All three
  of A's directions plus the aliveness rule verified in-class.
- **Consistency checks executed:** A's helpers (`c_circ`, `eta_contractive/expansive`) are exactly the
  symbols B's controls plant and catch; A's committed verdict ("equivariance wall") is the configuration B's
  referee names as corroborating; C's classification lemma (below) is the adjudication key and predicts
  precisely (construct = wall, no-go = holds). **No conflict anywhere.**

**Joint statement earned (toy-family grade):** condition 2′ is **closed negative at every modeled tier** —
deriving the oriented sector asymmetry from finality/issuance structure requires adding a flip-symmetry-
breaking primitive to the vocabulary, and that primitive *is* the posit. Escape routes, each named and
priced: (i) non-binary value alphabets (|V| ≥ 3, outside C's lemma), (ii) frameworks/vocabularies beyond the
toys (B's residue lives in vocabulary choice; native TaF/TI primitives' flip-equivariance untested at
formalism grade), (iii) flip-odd primitives offered as "legitimate finality-native structure" — per
Located-Is-Not-Forced, whether such a primitive is posit or physics is nature's question, not the toys'.

## The new independence result (lane A byproduct 1 — referee-confirmed)

**The arrow bit and the sector bit are independent ℤ/2 degrees of freedom at representation level.** The
extension arrow (time direction) pays only for time-reversal twins; it cannot touch sector twins, because
the two ℤ/2s act on different data (source vs target). Corollary at this grade: **even a completed TaF
physical arrow (T57 resolved) would not close condition 2′.** This sharpens lane D's base-vs-fiber
diagnosis: transport fails not for lack of a base asymmetry but because the base asymmetry is flip-even.
(Also confirmed: byproduct 3 — the edge-negation ℤ/2 of frustration is a different ℤ/2 from the fork flip;
flip-even relative-polarity structure cannot separate the orbit.)

## Lane C — the last hiding place: CLOSED for the named surface

At **shipped-code grade** against TaF's actual `d1_restriction_system.py`: `proposition_value` is
dereferenced in **exactly one function** of the entire module (`_overlap_test_passes`, a string-order
comparison — a label-reading atom); patch constraints, overlap tests, global sections, and restriction
morphisms (merges/splits) are otherwise value-blind. Plus the **binary-alphabet classification lemma**
(elementary but correct, all arities): **label-free = flip-even** — so every label-free patch predicate is
flip-even and cannot select the sign; every flip-odd predicate names a label. The discovery branch is closed
by classification, not by sampling. Riders (referee): the binary alphabet and the flip are fixture-imposed
encodings on T26's unconstrained string field; scalar/vector projections covered at source-audit grade only;
pre-registration is an assertion, not a mechanized receipt.

## Lane D — tooling: adopted, with its own bounding finding

`tests/neutrality_executed_harness.py` (RECORDED→EXECUTED upgrade with a vacuity guard that demonstrably has
teeth) and `tests/tef_check_tag_linter.py` ([T]/[E]/[F] enforcement; `--strict` required for headline-hygiene
gating on legacy files — referee D7). The GU-001 executed-neutrality demo is **regression-grade only** and
does NOT discharge swing-2 lane-1 D3: full-strength executed neutrality needs **source-side
label-parameterized entry points** (W206–W211 hardcode both branches; W210 needs numpy + an out-of-bundle
import) — a verified bounding finding, routed to GU by mailbox as a suggestion.

## Process ledger (third swing running the two-stage protocol)

The referee stage again caught real defects in every lane, including: a false exported theorem
(growth-blindness — counterexample `Q=(1,1,0,0)` inside the lane's own class; corrected to strict-positivity
scope), a false numeric receipts sentence (e^15 margin claim), [E]-census inflation in three of four lanes
(the burned class's third recurrence — now with the linter to mechanize it), and two false counts inside the
tooling lane itself. **Standing rule going forward: fixtures use the check-registry convention and are
linted with `--strict` before their reports may count evidence.**

## What P2C can now state (the program's current theorem-shape, honest form)

> At every tier modeled to date — value-blind profiles, field-valued transport, non-thin morphisms,
> partitioned transport, TaF's shipped T26 patch machinery, and every finality/issuance-native vocabulary
> in the declared class — the GU-shaped sign bit is **located as an untransported fiber and provably not
> derivable without a flip-symmetry-breaking primitive, whose addition is the posit itself.** The arrow of
> issuance and the sector bit are independent ℤ/2s, so resolving the arrow (T57) cannot supply the sector.
> Escapes are enumerated: larger alphabets, richer vocabularies, native-formalism tiers, and the physical
> question of whether nature supplies a flip-odd primitive — which is `bar(b)`-grade OPEN, exactly where
> GU-001 left it.

## Ranked next work (supersedes swing-2 SYNTHESIS ranking)

1. **Write the bounded fiber theorem up as P2C's first synthesis document** (`synthesis/`): the complete
   chain (ADAPTER2-01 → gate pipeline → adapter downgrades → (ℤ/2)^blocks → 2′ twins → T26 closure →
   arrow⊥sector), with every grade and escape route explicit. This is the repo-independent explanatory
   product the charter names — and per the swing-1 novelty verdict, frame as program synthesis, not
   standalone novelty.
2. **Hierarchy v0.2** (charter-core; unchanged from the top-5 assessment — now the program's main line,
   since the fiber wall is closed at toy-family grade).
3. **Second specimen import** (TI `OnlineIssuance^LC` or TaF packet; mailbox request for source-issued
   freezing).
4. **Convert legacy fixtures to the check-registry convention** (linter `--strict` clean) and adopt
   pre-registration receipts (two-phase commit: expectations first, then results).
5. Text-verify the five classical-core citations (unchanged).
