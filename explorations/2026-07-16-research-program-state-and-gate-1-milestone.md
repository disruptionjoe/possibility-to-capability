---
artifact_type: exploration
status: active
governance_role: research_program_state
constitutional: false
created: 2026-07-16
---

# Research-program state, gate-1 milestone, and ranked next work

Purpose: give the hourly Progress fan-out a clear, honest, well-scoped objective for this repo, and record
the current state of the founding joint-seam instance. The ratified charter is `governance/CHARTER.md`; this
note does not modify it. It records program state and worthy next work only.

## Program frame (per charter)

P2C studies the repo-independent possibility -> dynamics -> records -> access -> capability -> finality
hierarchy and the GU/TI/TaF joint-seam object, under the Located-Is-Not-Forced and No-Artificial-Success
rules. The founding instance is GU's C-operator grading sign `bar(b)` (Founding Packet #1).

## Corrected current state (honest, per GU-side correction ADAPTER2-01, 2026-07-16)

A GU-side crosswalk session attempted to identify `bar(b)` with the polarity of a TI/TaF finality axis and
briefly over-claimed it. The GU-side audit (ADAPTER2-01, independently re-verified) corrected it. Net state:

- **SURVIVES (candidate hard core):** a common **Harary-balance predicate** — XOR-satisfiable = signed-graph
  balanced = no negative cycle = ℤ/2 cocycle exact — recurs across the GU (Gate 2a), TaF (T39), and TI
  encodings *on a shared signed-graph input*. This is a shared **obstruction vocabulary / candidate
  interface**, classical (Harary), and repo-independent. It is **not** an isomorphism of the native GU/TI/TaF
  objects.
- **WITHDRAWN / OPEN:** `bar(b) = finality-axis polarity` (ratification withdrawn, back to OPEN); adapter #2 is
  a monotone **forgetful** functor only (it collapses the opposite-polarity fork to one profile), not
  branch-preserving; the two-adapter precondition is **not met**; "one posit, everything else forced" is
  **not earned**.
- **Correct shape:** the sign is a **ℤ/2 fiber over** the finality profile, not the direction of the finality
  axis. Finality is compatible with the sign but does not derive or canonically select it.

## The open belt (reopen burden = the program's content-in-waiting)

Reopen the identity only when one frozen construction supplies all of:

1. the native source and target categories (GU and TaF, their real objects/morphisms);
2. a non-constant, **branch-preserving** functor on those real morphisms (keeps opposite polarities distinct);
3. proof the target polarity is not an imported label;
4. a **physical** (not merely conditional) finality direction;
5. a non-circular map from that direction to the GU positive-norm branch; and
6. controls against global sign relabeling and constant/forgetful functors.

## Gate #1 milestone (first worthy work)

Build (or disprove) obligation (2): a **native branch-preserving functor** between the actual GU and TaF
categories, using TaF's real four-coordinate D1 (or D1Field), **with adversarial controls specified up front
this time**:

- PASS requires: opposite-polarity fork states map to *distinct, non-equal* target objects; identity and
  composition preserved on real morphisms; a constant/forgetful map is *rejected* by the same test; the
  polarity label is *not* inserted into the target.
- FAIL (equally valuable): the profile provably forgets the fiber for every value-blind target -> the honest
  conclusion is that the sign is a non-structural fiber, and the program's scope is the **consistency layer
  only** (the value is a posit). Bounding the program is a first-class result (Failure-Preservation Rule).

Do not report PASS without the constant-map control firing negative. (The prior error was labeling
source-common-successor-absence as target incomparability; both fork states actually shared one profile.)

## Open hypothesis (Joe, 2026-07-16) — candidate anchor for obligations (4)-(5)

**The positive-norm branch may be selected by the observer's continued participation in finality:** an
observer that is still "alive" keeps issuing/participating, so its direction is the observed (positive-norm)
one; a "dead" observer stops contributing. This is a **conjecture to test**, not a settled anchor. Test it
against the charter's **Neutrality gate**: does aliveness genuinely break the +/- relabeling symmetry by an
independent physical principle, or does it smuggle the label in? If it only relabels, it fails neutrality.

## Ranked next worthy work (for Progress selection)

**UPDATE 2026-07-16:** all four items below were executed by the 2026-07-16 big swing
(`explorations/2026-07-16-big-swing/`, four lanes, each adversarially refereed). Results, referee-corrected
grades, and the SUPERSEDING ranked list live in `explorations/2026-07-16-big-swing/SYNTHESIS.md`. Headlines:
(1) GU-001 is **IMPORTABLE at receiver-verified grade** (pin `77879e5`, three receiver annotations) — the
import itself is now the top-ranked action; (2) gate #1 resolved SPLIT at toy tier (profiles-only collapse is
definitional; D1Field functor exists with fiber equivariantly-transported-but-undetermined; **native-tier
remains open**); (3) novelty scan: NEW-FRAMING-ONLY near NOTHING-NEW (closest prior: Abramsky et al. 2015) —
do not write up the hard core standalone; (4) aliveness anchor: **FAILS-NEUTRALITY** at this grade (framework
theorem: the rule relabels, does not select), honestly parked with a precise 3-part revival condition; the
one open door is the Krein-dynamical norm-link. Fixtures persisted and reproduced in-repo:
`tests/gate1_branch_preserving_functor.py`, `tests/aliveness_neutrality_test.py` (both exit 0).

Original ranking (retained for provenance):

1. **Founding-packet provenance:** determine whether GU can freeze a source packet at a pinned revision that
   reflects the corrected status (`bar(b)` OPEN; five-method located-not-forced; adapter downgrade). If not,
   keep `GU-001` at `NOT_YET_IMPORTABLE` with the missing source gates named. (Charter Deliverable 1B.)
2. **Gate #1 attempt** with the adversarial spec above (native branch-preserving functor, or its disproof).
3. **Novelty scan** of the surviving hard core (the common Harary/consistency predicate) against relational
   QM, Quantum Darwinism, consistent histories, and DHR superselection — does the framing add anything, or is
   it a repackaging? (Determines whether there is a contribution under the interface.)
4. **Formalize + neutrality-test** the observer-aliveness anchor hypothesis.

## Honest bounds

No artificial joint success; source sovereignty preserved; Located-Is-Not-Forced; nothing here upgrades any
GU/TI/TaF claim. `bar(b)`, H59, Krein positivity, physical issuance, and the generation count remain open at
their source grades.
