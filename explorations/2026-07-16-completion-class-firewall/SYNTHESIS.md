---
artifact_type: exploration
status: frozen_record
created: 2026-07-16
constitutional: false
work_item: P2C-NULL-COMPLETION-CLOSURE
tier: provisional_synthesis
governing_referee_report: REFEREE-REPORT.md (in this directory; GOVERNS — read before citing)
---

# Completion-class closure swing: the firewall is DERIVED, and the composite attack comes within one signature

**Provisional synthesis at exploration tier. Nothing here is canon; nothing
is `proved` or `resolved` in the repo-canon sense; no source claim status
moves. The referee report in this directory governs.**

## 0. What this swing owed

The cross-repo adjudication (`explorations/2026-07-16-cross-repo-adjudication/`,
commit `512638a`) left this item carrying two named debts:

- **C2 / F1 live remainder (a):** the scoped survivor's firewall flank was
  standard-relative — the conclusion-class cap was TI CompletionClass v1's
  stipulated table, and P2C had no legitimate-completion class of its own.
  Question: is the firewall DERIVABLE from admissibility requirements a
  P2C-owned class needs anyway, REFUTABLE (a legitimate unrestricted absorber
  exists, reopening F1 at full strength), or genuinely axiomatic?
- **Referee D2:** compositions of local completions were never tested against
  the witness's (Q,I,P) operational delta; a composite local absorber was the
  strongest untested attack on the operational-survival leg.

Inputs, all read-only: the frozen witness bundle
(`exports/witness/P2C-W1-superconducting-ring-v0.1/`), the imported sovereign
packets (`packets/imports/TI-WFA-001/`, `packets/imports/TAF-002/`), and the
pre-existing P2C artifacts supplying the axioms' provenance. No cross-repo
action was taken; TI's v1 is consumed only as a frozen comparison point.

## 1. The class

CompletionClass-P2C v0.1 (`COMPLETION-CLASS-P2C.md`, this directory;
executable `tests/p2c_completion_class_closure.py`, frozen output
`closure_output.txt`): nine channel-type constructors (hidden-state,
boundary, seed, provenance, resource, history, access, relabeling, gauge —
the frozen witness's own local sweep), two containment-type constructors
(certified whole-family; after-fact hull), closed under finite sequential
composition, scored into three claim classes (representational, containment,
operational), governed by three admissibility axioms with pre-existing,
pre-this-swing P2C provenance:

- **A-FRAME** (frame/budget preservation) — witness `frame_declaration`
  (freeze `4c9c28b`) + adapter `RESOURCE_FRAME_CHANGED` typing (`850521c`);
- **A-UNIF** (outcome-independence; no verdict-carrying input) — adapter
  circularity control (`850521c`);
- **A-CAL** (verdict map non-constant in either direction) — verbatim this
  item's own pre-declared kill condition.

The axioms mention no constructor type. That is what makes the next section
a derivation rather than a relabeled stipulation.

## 2. Result 1 — the firewall verdict: DERIVED

**The conclusion-class firewall is DERIVABLE in the P2C-owned class** — a
theorem of the finite model, not a stipulated table entry (contrast: TI v1
assigns `whole_family -> global/ontological` by table). Two legs, both
executable, each with a load-bearing omission mutant:

1. **Uncertified containment (the fiat absorber).** The after-fact hull is
   TOTAL (`f1`). If containment discharged operational claims, uniformity
   (A-UNIF) would make the hull discharge every operational claim, so the
   operational verdict map would go constant — the mutant exhibits exactly
   this (`f2-fail`: the entire zoo "absorbed", including the null controls),
   violating A-CAL, i.e. dying by the item's own pre-declared kill condition.
   Hence uncertified containment caps at representational strength.
2. **Certified containment (the legitimate absorber).** The certified family
   genuinely contains the witness (`w1`, `f3` — the P2C-owned mirror of the
   frozen `k2` and TI's `e1`: superconductivity is a phase of a known
   Hamiltonian; no absolute-novelty claim survives). But its only would-be
   operational realizers are outside the A-FRAME-respecting channel menu: no
   in-frame composite reproduces the trace at any depth (`c1`, `c2`, `c4`).
   Dropping A-FRAME re-admits an unbudgeted feedback servo and the
   capability-positive class empties (`f3-fail`). Hence certified containment
   earns containment strength and cannot be upgraded in-frame.

Corollary recorded, not stipulated: identity-absorption ("just perform the
transition") needs no separate exclusion axiom — the transition IS the frame
change, and A-FRAME already types it.

**Refutation branch checked, not assumed away:** the candidate legitimate
unrestricted absorber (the hull, granted operational strength) was
implemented and run; it does reopen absorption — and simultaneously kills
the instrument (constant verdict map, `f2-fail`). So the F1-at-full-strength
reopening exists only inside standards that fail pre-declared instrument
admissibility. That is the derivation's exact content and its exact limit.

**Scope (binding):** the derivation binds every legitimacy standard that
functions as a non-constant, outcome-independent, frame-preserving
diagnostic instrument. A standard rejecting one of the three axioms is not
refuted; it is exhibited (executably) to be a constant or verdict-carrying
instrument. "Instrument admissibility" is itself a methodological
commitment — so C2 is NARROWED from "relative to TI v1's frozen table" to
"relative to three pre-declared instrument-admissibility axioms", not
eliminated.

## 3. Result 2 — composition closure (referee D2): the survivor holds, by exactly one signature

The class was closed under declared sequential composition and the closure
verified as a fixpoint (`c4`: depth 4 adds no profile beyond depth 3, so the
results below hold for ALL finite composites, not an enumeration bound).

- **No composite absorbs the witness** (`c1`): none of the 819 enumerated
  composites (exhaustive at fixpoint) reproduces the quantized, locally
  invariant, persistent staircase in the N frame at matched budget. The
  referee's D2 gap is closed at this model granularity: composite local
  absorbers — including the named seed + hidden-state + history shapes —
  fail like their components. Epistemic parity (referee R2): this composite
  result is the SAME evidence type as the frozen `k1` it extends — a
  P2C-authored finite model over literature-grade physics — so it closes D2
  at exactly the grade at which D2 was opened, no higher.
- **The composition machinery is not vacuous** (`c3`): the register target
  (quantized, persistent, NOT locally invariant) is absorbed at depth 2
  (`hidden_state` then `access`) and by NO single kind. The closure strictly
  exceeds the union of the singles — so the witness's composite survival is
  a result about a genuinely stronger attack, not a re-run of `k1`.
- **The margin is one signature wide** (`c1-fail`, headline honesty): drop
  local-op invariance from the matcher and a composite absorber immediately
  appears. Composites reach quantization AND persistence; the entire escape
  now hangs on local-op invariance alone (I — the winding's insensitivity to
  gauge shift, relabeling, and latent-state perturbation, backed at
  literature grade by the 1961 fluxoid experiments via D-I).
- **The escape is theorem-shaped, not by-construction** (`c2`, carrier
  dichotomy): every locally-invariant composite readout is the null value —
  any flux-tracking carrier in the N frame is either physical (decays,
  perturbable) or latent (perturbable; latent perturbation is a member of
  the frozen witness's local-op group). Invariance-with-value-tracking is
  unreachable at any composition depth. This is the nontrivial no-go target
  the item's contract demanded: the witness escapes because of a structural
  dichotomy about value carriers, not because the class was written to
  exclude it.

## 4. Controls and kill conditions (both directions, executable)

- **Kill direction 1 (trivially narrow): NOT triggered.** Ordinary novelty
  is absorbed (`p1`): the revealed eddy, the relaxed trace, and the
  composite-only register target all earn operational absorption.
- **Kill direction 2 (unrestricted): NOT triggered.** The alien third-quantum
  staircase is neither operationally absorbed nor certified-contained
  (`p2`, `f1`) — it stays a first-class unexplained residual, and the
  certified family demonstrably excludes things (certification has bite).
- Omission mutants: each axiom is load-bearing by exhibition (`f2-fail`
  A-CAL/A-UNIF leg, `f3-fail` A-FRAME leg, `f4-fail` A-UNIF containment
  leg). Matcher teeth: `c1-fail`.
- Validation: fixture exit 0; `tef_check_tag_linter.py --strict` clean;
  headline 11 [E] + 4 [F] = 15 evidential checks, 2 [T] listed separately.

## 5. What this does to the standing records (receiver-owned bookkeeping)

- **F1 (fixed-family absorber):** live remainder (a) — "whether a P2C-owned
  legitimate completion class must and can carry an equivalent
  conclusion-class restriction non-circularly" — is DISCHARGED at
  exploration tier, model-relative: the restriction is derived from
  type-free, pre-declared axioms, so it is not ad hoc. Remainder (b)
  narrows: a rival standard without a firewall must reject one of the three
  instrument axioms, and each rejection has an exhibited collapse. Remainders
  (c) (individuation fork, C4) and (d) (general family-exhaustiveness, TI
  R-2) are UNTOUCHED and stay live. F1 overall: **LIVE, FURTHER NARROWED** —
  now carried by the fork/transfer legs, no longer by the legitimacy-standard
  leg.
- **C2 (standard-relativity of the scoped survivor):** narrowed as in
  section 2. The scoped survivor's firewall flank no longer rests on TI v1's
  authority; it rests on P2C's own derived firewall, with the residual
  methodological relativity named. TI's v1 cap and the v0.1 derived cap
  agree in content; this convergence is correlated through shared subject
  matter (v1 was read before v0.1 was built) and is NOT counted as
  independent evidence.
- **Referee D2 (composite absorbers):** closed at this model granularity,
  with the one-signature margin recorded. Any finer-grained witness model
  (Alternate B gamma = log D; Alternate C BEC) must re-run the composite
  attack — the frontier result (composites reach Q and P) says the attack is
  live wherever I is weaker.
- **Scoped-survivor disposition:** unchanged in polarity — the witness still
  survives at operational class and is still absorbed at containment
  (global/ontological) class — but the completion leg is now backed by a
  P2C-owned standard closed under composition, and the survival is known to
  be one-signature-deep.
- **Re-arm trigger (recorded, no action):** `P2C-CROSS-REPO-ADJUDICATION`'s
  stated re-arm condition — "a P2C-owned legitimate completion class ready
  for a second-standard re-adjudication of P2C-W1" — is now MET. The
  re-adjudication is a LOCAL swing (frozen imports read-only; no new
  cross-repo action, no proposals sent this swing).

## 6. Honest limits

- Everything is a finite, formal-only model; the physics is stipulated at
  literature grade, unchanged from the frozen witness. The carrier
  dichotomy's physical force = the commitment that a normal conductor has no
  locally invariant, flux-tracking, zero-maintenance channel — the same
  grade as the frozen `k1`, not a new measurement.
- The model is coarse: three distinct composite profiles. The enumeration is
  exhaustive (fixpoint-verified) but combinatorially shallow; the evidential
  weight is in the closure structure, the mutants, and the derivation
  exhibits, not in search difficulty.
- The steelman granted the rival the realized staircase value for free
  (deterministic model, so outcome-independence had no bite at the channel
  level — it bites only at the family-declaration level). This strengthens
  the composite attack, so it cuts against the survivor, not for it.
- A-CAL's authority is the item's own pre-declared kill condition;
  derivation scope as in section 2. Axiomatic-residue statement: what
  remains genuinely posited is "the class must function as a diagnostic
  instrument" — not the firewall itself.
- The `f3-fail` servo mutant's physical reading (unbounded-budget feedback
  emulation) is informal-literature grade.
- Certification's outcome-independence for P2C-W1 rests on the
  literature-grade fact that the S-phase signature was fixed by physics
  (BCS 1957; fluxoid experiments 1961) before any candidate was scored;
  in-model it is a declaration (referee R3). General family-exhaustiveness
  is not proved here, matching TI v1's honest limit.

## 7. Files and receipts

- `COMPLETION-CLASS-P2C.md` (this directory) — the class contract, testable.
- `tests/p2c_completion_class_closure.py` — executable; exit 0; strict-lint
  clean.
- `closure_output.txt` (this directory) — frozen fixture output.
- `REFEREE-REPORT.md` (this directory) — adversarial pass; GOVERNS.
