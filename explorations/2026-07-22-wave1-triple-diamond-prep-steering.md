---
title: "Directed-wake steering — Wave-1 triple-diamond prep (P2C's three high-value items)"
status: steering-input
doc_type: directed-wake-steering
created: 2026-07-22
source: Wave-1 triple-diamond prep (Joe-directed); full portfolios in the wave-1 workflow output
boundary: steering input for the hourly cadence / steward. No claim/canon/verdict/freeze moved. Prep only — falsifiers predeclared; evidence disposes.
---

# P2C — directed-wake steering (Wave-1 prep)

**Cross-repo frame:** P2C-2 is one of THREE FACES of a single self-generating-source object shared with
TI-1 and TaF-1 — the deep identity is **static-representability (P2C) = computable-pre-commitment (TI) =
coordinate-pre-commitment (TaF)**: *can the next step be pre-committed by a fixed finite/static/computable
rule on the recorded past?* P2C-2 is the **temporal** instance (a response with no static-Hamiltonian
representative). The σ thread (P2C-1) is a **separate, orthogonal axis** — orientation/polarity, NOT source
— and caps at forcing-the-distinction, never forcing-the-selection. Guard: these advance NOW on native
substrate; **do NOT re-gate on external (σ) issuance**; "relocation of a stuck seam" ≠ "resolution."

## P2C-2 — dynamical transition witness  · DOABLE-NOW, fully native (top native swing)
- **Reframe:** dynamical-ness is the VEHICLE, not the goal. Target = a native response with **no
  static-Hamiltonian representative** (defeats the thin-shadow absorber F2) that is a task-set delta.
- **Wake directive:** build the exact **TFIM-quench DQPT** fixture with the thin-shadow-reconstruction kill
  FIRST (cheapest program-premise probe), then the **matched-drive-power anomalous-Floquet** specimen as the
  real swing; run the **MIPT `classify_transition` pre-kill** as a 10-min triage before either.
- **Payoff:** even undischarged, the Floquet champion RELOCATES the stuck cross-repo whole-family seam into
  an in-repo matched-resource question P2C can adjudicate. Fully unblocked; new files only, deterministic.

## P2C-1 — σ Door-4 adjudication frame  · frame DOABLE-NOW; verdict doubly-gated
- **Reframe:** an admissibility-AND-forcing criterion for a NON-transition finality witness. Honest ceiling
  (adversarial pass, partly decisive): a topological invariant can force the flip-odd/flip-even DISTINCTION
  (owner-identified) but **never the SELECTION of which sign nature runs** — max verdict is (iii), never (iv).
- **Wake directive:** prereg the staged gate at `explorations/2026-07-22-sigma-door4-adjudication/`
  (Stage-0 battery flip-parity on w₁ + Stage-1 counterterm/structure-forced audit), four predeclared
  verdicts, the ceiling stated out loud; **σ value stays GU-gated NOT_YET_IMPORTABLE**; new files only,
  no number computed. Fixture must ABSTAIN if asked to assert σ≠0 without a frozen packet.

## P2C-3 — cross-domain transfer battery  · DOABLE-NOW, read-only
- **Reframe:** the North Star's OWN falsifier ("definitions can't transfer beyond one domain without ad hoc
  reinterpretation") has never been run. Deliverable genre is EVIDENCE-DECIDED (the math core is already
  near-NOTHING-NEW; only the agent-capability METHODOLOGY is candidate-novel, and unscanned).
- **Wake directive:** ONE swing = a methodology-level **novelty/genre scan** (vs Halpern-Pearl, control-
  theory reachability, object-capability security, process ontology, preregistration, agent-eval;
  adversarial referee governs) + a domain-neutral **transfer-BREAKER** battery prereg skeleton, **security
  as domain 1**, default forecast "transfer fails." Read-only; do NOT build fixtures until the genre gate
  returns. This is the campaign's single highest value-per-token gate.
- **UNIVERSALITY REFRAME (Joe, 2026-07-22 — supersedes the "novelty-or-bust" read):** the transfer battery's
  value is a **cross-domain UNIVERSALITY contribution**, not only a novel primitive. A "same typed structure
  / same math as field X" result is a **POSITIVE universality data point** (complexity-science mode: the
  coarse invariant that recurs), NOT a novelty failure — do NOT file it NOTHING-NEW-and-abandon. The
  genuinely-unclaimed object is the possibility→…→finality **ladder as one domain-neutral instrument** (the
  scan found it unpackaged anywhere). Claim at the **coarse-invariant level** (P2C is the common denominator,
  not the richest per-domain description — object-capability is genuinely richer in security), and prioritize
  domains where the universality is **non-trivial / forcing**, not endless "holds here too" confirmations.
  Pursue P2C-3 as a cross-domain universality result + a reusable cross-domain audit instrument. The
  2026-07-22 security transfer-breaker (HOLDS, non-circular, same verdict as object-capability) is, under
  this lens, the FIRST confirmed cross-domain identity — a positive datum, not a strike.

**Full candidate portfolios + predeclared falsifiers:** Wave-1 triple-diamond prep output (2026-07-22).
